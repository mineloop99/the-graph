{
  "name": "blocks",
  "license": "UNLICENSED",
  "files": [
    "generated"
  ],
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service mineloop99/aurora-blocks",    
    "prepare:fantom": "mustache ../../config/fantom.json subgraph.yaml > subgraph.yaml"
  }
}
